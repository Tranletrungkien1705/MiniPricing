# DEEPEN-LOG — MiniPricing

- 2026-XX-XX: Port nghiệp vụ **Giá theo quy cách (Mst_SpecPrice)** từ nguồn `2019.4.ProductCenter` (Dev/V20/01.Release, SVN pristine). Nguồn: entity `Mst_SpecPrice` (SpecCode+UnitCode+NetworkID, BuyPrice/SellPrice/DiscountVND/VATRateCode/CurrencyCode, EffectDTimeStart/End), controller `MstSpecPriceController` (Get/Create/Update/Delete), error codes `Mst_SpecPrice_CheckDB_*`. Thêm `SpecPrice` entity + `SpecPriceService` (upsert/list/delete + resolve giá hiệu lực tính giá sau chiết khấu và giá gồm VAT) + endpoints `/api/specprices`, `/api/specprice` + seed mẫu. Build Release: 0 error.
- 2026-XX-XX: Port nghiệp vụ **Giá xe theo CarSubSpec (Mst_CarSubSpecPrice)** từ nguồn `2019.4.ProductCenter` (SVN pristine). Nguồn: error codes `ErrProductCenter.Mst_CarSubSpecPrice_Save_InputTblNotFound`/`_InvalidValue` ("Không tìm thấy GTĐG, GTBĐTĐ trong hệ thống") và `Mst_CarSubSpecPrice_CheckDB_CarSubSpecPriceNotFound`; pattern theo `Mst_SpecPrice` (controller/model đã có trong checkout). Thêm `CarSubSpecPrice` entity (CarCode+SubSpecCode+NetworkID, GTDG/GTBDTD/SellPrice, EffectDTimeStart/End) + `CarSubSpecPriceService` (upsert/list/delete + resolve giá hiệu lực trả giá bán và chênh lệch so với GTĐG/GTBĐTD) + endpoints `/api/carsubspeccprices`, `/api/carsubspeccprice` + seed mẫu. Build Release: 0 error.
- 2026-XX-XX: Port nghiệp vụ **Danh mục thuế suất VAT (Mst_VATRate)** từ nguồn `2019.4.ProductCenter` (Dev/V20/01.Release, SVN pristine). Nguồn: entity `Mst_VATRate` (VATRateCode+NetworkID, VATRate, VATDesc, FlagActive), controller `MstVATRateController` (Get/Create/Update/Delete), business `WAS_Mst_VATRate_*` + `Mst_VATRate_CheckDB`, error codes `Mst_VATRate_Create_InvalidVATRateCode`/`Mst_VATRate_CheckDB_VATRateCodeNotFound`/`_VATRateCodeExist`/`_FlagActiveNotMatched`. Thêm `VatRate` entity + `VatRateService` (upsert/list/delete + resolve thuế suất hiệu lực theo mã+kênh) + endpoints `/api/vatrates`, `/api/vatrate` + seed mẫu (VAT0/5/8/10). Build Release: 0 error.
- 2026-XX-XX: Port nghiệp vụ **Tỷ giá ngoại tệ (Mst_CurrencyEx)** từ nguồn `2019.4.ProductCenter` (Dev/V20/01.Release, SVN pristine). Nguồn: entity `Mst_CurrencyEx` (CurrencyCode+NetworkID, CurrencyName, BaseCurrencyCode, BuyRate/SellRate, UpdatedTime, InterEx, Remark), controller `MstCurrencyExController` (Get/Create/Update/Delete), business `WAS_Mst_CurrencyEx_*` + `Mst_CurrencyEx_CheckDB`, error codes `Mst_CurrencyEx_CheckDB_CurrencyCodeNotFound`/`_CurrencyCodeExist`/`_FlagActiveNotMatched`, `Mst_CurrencyEx_Create_InvalidCurrencyName`; bảng lịch sử `Mst_CurrencyExHist`. Thêm `CurrencyEx` entity + `CurrencyExService` (upsert/list/delete + resolve tỷ giá hiệu lực theo mã+kênh + convert quy đổi số tiền sang tiền tệ gốc theo tỷ giá mua/bán) + endpoints `/api/currencyexes`, `/api/currencyex`, `/api/currencyex/convert` + seed mẫu (VND/USD/EUR). Build Release: 0 error.
- 2026-XX-XX: Port nghiệp vụ **Quy cách × đơn vị tính (Mst_SpecUnit)** từ nguồn `2019.4.ProductCenter` (Dev/V20/01.Release, SVN pristine). Nguồn: entity `Mst_SpecUnit` (SpecCode+UnitCode+NetworkID, StandardUnitCode, SpecUnitDesc, Qty, Length/Width/Height/Volume/Weight, Remark, FlagActive), controller `MstSpecUnitController` (Get/Create/Update/Delete), business `WAS_Mst_SpecUnit_*` + `Mst_SpecUnit_CheckDB`, error codes `Mst_SpecUnit_CheckDB_SpecUnitNotFound`/`_SpecUnitExist`/`_FlagActiveNotMatched`; Create kiểm tra Spec/Unit/StandardUnit tồn tại & active. Thêm `SpecUnit` entity + `SpecUnitService` (upsert/list/delete + resolve đơn vị tính hiệu lực theo quy cách+đơn vị+kênh + convert quy đổi số lượng về đơn vị chuẩn theo hệ số Qty) + endpoints `/api/specunits`, `/api/specunit`, `/api/specunit/convert` + seed mẫu (SP-001 CAI/THUNG, SP-002 BO). Build Release: 0 error.
- 2026-XX-XX: Port nghiệp vụ **Lịch sử giá theo quy cách (Mst_SpecPriceHist)** từ nguồn `2019.4.ProductCenter` (Dev/V20/01.Release, SVN pristine). Nguồn: bảng audit `Mst_SpecPriceHist` (OrgID, SpecCode, UnitCode, NetworkID, BuyPrice, SellPrice, CurrencyCode, VATRateCode, DiscountVND, EffectDTimeStart/End, Remark, FlagActive, LogLUDTimeUTC/By, FunctionName, FunctionActionType, FunctionRemark, HistRefType, RefCode00..03) + business `Mst_SpecPriceHist_Perform` trong `BizProductCenter` (mỗi Create/Update/Delete của `WAS_Mst_SpecPrice_*` ghi 1 snapshot, HistRefType='MST_SPECPRICE', action type `TConst.FunctionActionType` = ADD/UPDATE/DELETE). Thêm `SpecPriceHist` entity + `SpecPriceHistService` (record/list/timeline + chuẩn hoá action type) + tích hợp ghi lịch sử vào `SpecPriceService` (Create→ADD, Update→UPDATE, Delete→DELETE) + endpoints `/api/specpricehists`, `/api/specpricehist/timeline` + seed mẫu. Build Release: 0 error.
